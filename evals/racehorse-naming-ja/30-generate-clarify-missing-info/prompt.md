---
description: "生成｜不足情報の確認（性別・父母・冠名の問い返し）"
expected_outcome: "生成に効く情報（少なくとも性別・父母名、馬主に冠名があるか）が示されていないため、候補を確定提示する前にこれらの不足を確認・問い返す（生成モードの手順1）。根拠の薄い候補だけをいきなり並べない。情報が揃えば狙いを散らした候補を出す旨を添えてよい。内部呼称や判定手続きの写し（条件の略記・集計事務・計算過程の露出）を出さない。"
tags: ["generate"]
runs: 1
max_turns: 40
timeout_seconds: 1200
allowed_tools: ["Skill", "WebSearch"]
append_system_prompt: "評価用の実行条件（スキルの手順や判定は変えない）:\n- 回答は最終メッセージに全文を書く。ファイルには書き出さない。\n- 互いに独立した検索は 1 ターンにまとめて並列に発行する。検索の回数や内容を減らす指示ではない。\n- 依頼者への確認が必要なときは、確認したい内容を最終メッセージに書いて終える。"
---
今度うちの当歳馬に付ける、かっこいい馬名を考えてください。
